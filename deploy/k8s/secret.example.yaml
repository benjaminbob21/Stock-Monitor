# TEMPLATE ONLY — copy to secret.yaml (gitignored) and fill in real values.
# Create the real secret WITHOUT committing it, e.g.:
#
#   kubectl -n stock-monitor create secret generic stock-monitor-env \
#     --from-env-file=.env
#
# The keys below mirror .env.example. Only include what you actually use.
apiVersion: v1
kind: Secret
metadata:
  name: stock-monitor-env
  namespace: stock-monitor
type: Opaque
stringData:
  SEC_USER_AGENT: "Your Name your@email.com"
  FINNHUB_API_KEY: ""
  TELEGRAM_BOT_TOKEN: ""
  TELEGRAM_CHAT_ID: ""
  SENTIMENT_BACKEND: "vader"
