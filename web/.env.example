# Local env for the web app. Copy to .env.local (gitignored) if you change the API URL.
# Server-side URL the Next.js proxy route uses to reach the FastAPI backend.
STOCK_MONITOR_API_URL=http://127.0.0.1:8137
