[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-monitor"
version = "0.1.0"
description = "Personal, explainable ML stock research & recommendation tool (human-in-the-loop, no auto-trading)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = [
    "numpy",
    "pandas<3",
    "lxml",
    "scikit-learn",
    "lightgbm",
    "shap",
    "pandas-ta>=0.4.71b0",
    "yfinance",
    "requests",
    "requests-cache",
    "pydantic",
    "pydantic-settings",
    "pandera",
    "duckdb",
    "mlflow",
    "fastapi",
    "uvicorn[standard]",
    "apscheduler",
    "tenacity",
    "vaderSentiment",
    "prometheus-client",
]

[project.optional-dependencies]
dev = ["pytest", "ruff", "mypy", "httpx"]
finbert = ["transformers", "torch"]

[project.scripts]
stock-monitor = "stock_monitor.cli:main"
stock-monitor-train = "stock_monitor.pipeline:main"
stock-monitor-validate = "stock_monitor.evaluation:main"
stock-monitor-backtest = "stock_monitor.backtest:main"
stock-monitor-scan = "stock_monitor.scan:main"
stock-monitor-scheduler = "stock_monitor.scheduler:main"
stock-monitor-backfill = "stock_monitor.backfill:main"
stock-monitor-short-train = "stock_monitor.short_cli:main"
stock-monitor-short-alerts = "stock_monitor.short_cli:alerts_main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
warn_unused_ignores = true
warn_redundant_casts = true
ignore_missing_imports = true
files = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["src"]
